experiment_name: 'test_unsupervsied_depth'

model:
    type: 'monodepth2'

    encoder:
        params:
            nof_layers: 50
            weights_init: 'pretrained'

    pose_net:
        input: 'pairs' # 'all'
        params:
            nof_layers: 18
            weights_init: 'pretrained'

datasets:
    paths_to_configs : ['C:\Users\benba\Documents\University\Masterarbeit\Depth-Semantic-UDA\cfg\yaml_files\train\depth\cityscapes_sequence.yaml',
                        'C:\Users\benba\Documents\University\Masterarbeit\Depth-Semantic-UDA\cfg\yaml_files\train\depth\cityscapes_semantic.yaml']
    loss_weights : [1, 0]

train:
    nof_epochs: 40
    batch_size: 3

    optimizer:
        type: 'Adam'
        learning_rate: 0.0001
    scheduler:
        type: 'MultiStepLR'
        gamma: 0.5
        milestones: [30] # half the learning rate for the last 10 epochs

val:
    batch_size: 1
    nof_workers: 4

checkpoint:
    use_checkpoint: False

device:
    multiple_gpus: False

io:
    path_save: 'C:\\Users\\benba\\Documents\\University\\Masterarbeit\\models\\test'
